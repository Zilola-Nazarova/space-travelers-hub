import '../styles/App.css';

const App = () => (
  // template code to see if styles are imported correctly
  // and ESLint is actually working, feel free to delete later
  <h1 className="App">Example</h1>
  // here is the space for our Routes
);

export default App;
